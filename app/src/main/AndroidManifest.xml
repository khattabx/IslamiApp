<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Islami"
        tools:targetApi="31">
        <activity
            android:name=".islamiproject.main.HadethDetailes"
            android:exported="false"
            android:label="@string/title_activity_hadeth_detailes"
            android:theme="@style/Theme.Islami"
        />
        <activity
            android:name=".islamiproject.main.SuraDetailes"
            android:exported="false"
            android:label="@string/title_activity_sura_detailes"
            android:theme="@style/Theme.Islami"
        />
        <activity
            android:name=".islamiproject.main.Home"
            android:exported="false" />
        <activity
            android:name=".islamiproject.main.Splash"
            android:exported="true"
            android:theme="@style/nobar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>